# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/.terraform.lock.hcl
**/errored.tfstate

# Secrets — never commit
**/terraform.tfvars

# Plans
*.tfplan

# OS
.DS_Store
CREDENTIALS.txt
helm/monitoring/Chart.lock
helm/monitoring/charts/
