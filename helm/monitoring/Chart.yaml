apiVersion: v2
name: monitoring
description: Prometheus + Grafana + AlertManager for DataPlatform
type: application
version: 0.1.0
dependencies:
  - name: kube-prometheus-stack
    version: "69.3.2"
    repository: "https://prometheus-community.github.io/helm-charts"
