apiVersion: v2
name: airflow
description: Apache Airflow with KubernetesExecutor for DataPlatform
type: application
version: 0.1.0
dependencies:
  - name: airflow
    version: "1.16.0"
    repository: "https://airflow.apache.org"
