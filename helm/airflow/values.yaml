airflow:
  executor: KubernetesExecutor

  nodeSelector:
    role: system

  serviceAccount:
    create: false
    name: airflow

  ingress:
    web:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
        nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
      hosts:
        - name: airflow.gusnikola.com
          tls:
            enabled: true
            secretName: airflow-tls

  postgresql:
    enabled: true
    image:
      tag: latest
    primary:
      nodeSelector:
        role: system
      persistence:
        size: 5Gi
        storageClass: gp2
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 512Mi

  scheduler:
    nodeSelector:
      role: system
    serviceAccount:
      annotations:
        eks.amazonaws.com/role-arn: "arn:aws:iam::866376946262:role/dataplatform-dev-airflow-irsa"
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 1000m
        memory: 1Gi

  webserver:
    nodeSelector:
      role: system
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 1000m
        memory: 1Gi

  triggerer:
    nodeSelector:
      role: system
    serviceAccount:
      annotations:
        eks.amazonaws.com/role-arn: "arn:aws:iam::866376946262:role/dataplatform-dev-airflow-irsa"
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi
    persistence:
      storageClassName: gp2

  dags:
    gitSync:
      enabled: true
      repo: https://github.com/GusNikola/DataPlatform-Infra.git
      branch: main
      subPath: dags
      period: 60s

  logs:
    persistence:
      enabled: true
      size: 5Gi
      storageClassName: gp2

  config:
    core:
      load_examples: "False"
    kubernetes_executor:
      namespace: airflow
      delete_worker_pods: "True"

  extraEnv: |
    - name: AWS_DEFAULT_REGION
      value: eu-central-1
